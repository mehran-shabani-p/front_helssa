# Project Structure

- `app/`: Application configuration, themes, and routing.
- `core/`: Shared utilities, network, storage, and design system components.
- `features/`: Modular feature folders each containing `data/`, `domain/`, and `presentation/` layers.
