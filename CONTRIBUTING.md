# Contributing Guidelines

- Use Conventional Commits for all commit messages.
- Fork the repository and create feature branches: `feature/<scope>-<short-title>`.
- Ensure `flutter analyze` and `flutter test` pass before submitting a pull request.
